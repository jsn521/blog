var html = require('./html.js');
var encoder = new (require('node-html-encoder').Encoder);

console.time('test');
for (var i = 0; i < 200000; i++) {
    html.decode('&ampersand&notin;&notit;', false);
    // html.encode('\tdurréé<wawaасадзжжж"', true);
}
console.timeEnd('test');

console.time('test');
for (var i = 0; i < 200000; i++) {
    encoder.htmlDecode('&ampersand&notin;&notit;');
    // encoder.htmlEncode('\tdurréé<wawaасадзжжж"');
}
console.timeEnd('test');